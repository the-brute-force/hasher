# Hasher  
Renames files in a directory.

# Features  
Able to hash in parallel.

# Usage  
A Markdown conversion of the man page is available [here](hasher.md)

# Building  
`make` compiles the project.  
`make install` installs the project.
